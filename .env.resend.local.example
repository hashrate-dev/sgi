# Copiá a .env.resend.local (npm run resend:init). No subas .env.resend.local a git.
#
# Agregá UNA línea sin # (obligatoria para envío real). La clave sale solo de resend.com (re_…).
# No anteponas re_ a un token de Vercel (vcp_…): eso no funciona (re_vcp_… inválido).
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxx
#
# Opcional (dominio verificado en Resend). Si no ponés FROM, el servidor usa onboarding@resend.dev.
# RESEND_FROM_EMAIL=HashrateSpace <noreply@hashrate.space>
#
# Remitente solo del reset de contraseña (si no está, usa RESEND_FROM_EMAIL / onboarding).
# Requiere dominio verificado en Resend para que no caiga a onboarding@resend.dev.
# PASSWORD_RESET_FROM_EMAIL=HashrateSpace <sales@hashrate.space>
#
# Si 1/true: en producción, ante error de “testing” de Resend, copia del enlace a MARKETPLACE_NOTIFY_EMAIL_TO.
# En local, si Resend falla, el relay a sales@ es respaldo (Resend de prueba no entrega a mails arbitrarios).
# PASSWORD_RESET_RESEND_SANDBOX_RELAY=0
#
# Para que el reset llegue al MAIL DEL USUARIO en local: definí PASSWORD_RESET_SMTP_* (Workspace/Gmail SMTP).
# En NODE_ENV distinto de production el servidor intenta SMTP a ese correo ANTES que Resend (así no dependés del relay a sales@).
# Alternativa sin SMTP: en Resend verificá el dominio hashrate.space y usá PASSWORD_RESET_FROM_EMAIL con ese dominio.
# PASSWORD_RESET_SMTP_HOST=smtp.gmail.com
# PASSWORD_RESET_SMTP_PORT=587
# PASSWORD_RESET_SMTP_SECURE=false
# PASSWORD_RESET_SMTP_USER=tu_cuenta@gmail.com
# PASSWORD_RESET_SMTP_PASS=contraseña_de_aplicación
# PASSWORD_RESET_SMTP_FROM=HashrateSpace <sales@hashrate.space>

# Si los avisos no llegan a sales@hashrate.space en prueba, destino explícito (tu inbox o prueba Resend):
# MARKETPLACE_NOTIFY_EMAIL_TO=sales@hashrate.space
